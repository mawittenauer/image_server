# .env.example
# Copy to .env and adjust if desired.

# Port the server listens on
PORT=3000

# Folder (relative to project root) containing images.
# MUST be within /public so images are actually served as static files.
IMAGE_DIR=public/images
